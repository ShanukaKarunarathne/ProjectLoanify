{
  "name": "your-app",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  },
  "scripts": {
    "build:functions": "esbuild functions/getCreditReport.js --bundle --outdir=dummy/ --platform=node --external:dotenv --external:node-fetch"
  }
}
